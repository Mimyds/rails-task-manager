<h1> My tasks  </h1>
<ul>
<% @tasks.each do |task| %>
<li><i class="far fa-check-square"></i> <%= link_to task.title, task_path(task.id)%> • <%= link_to "Delete", task_path(task), method: :delete, data: { confirm: "Are you sure? 🤨" } %></li>
<% end %>
</ul>
